spring:
  application:
    name: bookingsystem

  datasource:
    url: jdbc:postgresql://localhost:5432/EDI
    username: postgres
    password: 12410030

  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        dialect: org.hibernate.dialect.PostgreSQLDialect
    
server:
  port: 8080

# app:
#   cors:
#     allowed-origins: http://localhost:3000
#     allowed-methods: GET, POST, PUT, DELETE, OPTIONS
#     allowed-headers: "*"